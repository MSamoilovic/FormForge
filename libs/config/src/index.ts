// Field defaults configuration
export {
  FIELD_DEFAULTS,
  PHONE_FIELD_DEFAULTS,
  COLOR_PICKER_DEFAULTS,
  NUMBER_FIELD_DEFAULTS,
  RICH_TEXT_DEFAULTS,
  SELECT_FIELD_DEFAULTS,
  LIKERT_SCALE_DEFAULTS,
} from './field-defaults.config';

// UI configuration
export {
  UI_CONFIG,
  BORDER_RADIUS,
  SPACING,
  TRANSITIONS,
  TRANSITION_PRESETS,
  SHADOWS,
  Z_INDEX,
} from './ui.config';

